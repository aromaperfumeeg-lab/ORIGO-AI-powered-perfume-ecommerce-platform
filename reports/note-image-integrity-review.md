# مراجعة سلامة صور النوتات

هذه مراجعة للكود والربط والملفات، وليست شهادة بأن جميع الصور القديمة خضعت لفحص بصري. تبقى المهمة الشاملة مفتوحة حتى اكتمال مراجعة المحتوى.

## نتيجة الدفعة 58

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch58-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1343 نوتة مزودة بصورة صالحة للعرض، و522 نوتة بلا صورة، وصفر في انتظار المراجعة، و1353 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 89.

## نتيجة الدفعة 57

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch57-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1327 نوتة مزودة بصورة صالحة للعرض، و538 نوتة بلا صورة، وصفر في انتظار المراجعة، و1337 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 88.

## نتيجة الدفعة 56

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة ومتوازية، وأعيد توليد صورة شجرة العشب بعد أن كشف الفحص الأول غياب شفافية Alpha الحقيقية. راجعت مطابقة الدفعة بصريًا في `batch56-review.jpg`. جميع الملفات النهائية WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1311 نوتة مزودة بصورة صالحة للعرض، و554 نوتة بلا صورة، وصفر في انتظار المراجعة، و1321 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 87.

## نتيجة الدفعة 55

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch55-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1295 نوتة مزودة بصورة صالحة للعرض، و570 نوتة بلا صورة، وصفر في انتظار المراجعة، و1305 ملفات WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 85.

## نتيجة الدفعة 54

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch54-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1279 نوتة مزودة بصورة صالحة للعرض، و586 نوتة بلا صورة، وصفر في انتظار المراجعة، و1289 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 84.

## نتيجة الدفعة 53

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch53-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1263 نوتة مزودة بصورة صالحة للعرض، و602 نوتة بلا صورة، وصفر في انتظار المراجعة، و1273 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 83.

## نتيجة الدفعة 52

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch52-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1247 نوتة مزودة بصورة صالحة للعرض، و618 نوتة بلا صورة، وصفر في انتظار المراجعة، و1257 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 82.

## نتيجة الدفعة 51

أضيفت 16 صورة جديدة عبر 16 طلب ImageGen مستقلاً ومتوازيًا، وراجعت مطابقتها البصرية للنوتات في `batch51-review.jpg`. جميع الملفات WebP شفافة فعليًا بمقاس 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة في بصمة الملف أو المحتوى أو البصمة الإدراكية بين الصور الجديدة أو مع الملفات المسجلة. نجحت اختبارات المكتبة والهوية وعددها 34 اختبارًا.

الحالة الحالية: 1231 نوتة مزودة بصورة صالحة للعرض، و634 نوتة بلا صورة، وصفر في انتظار المراجعة، و1241 ملف WebP اجتازت جميعها الفحص التقني. رُفع رقم كاش المكتبة إلى 81.

## نتيجة الدفعة 43

أضيفت 16 صورة جديدة بطلبات ImageGen مستقلة، مع مراجعة بصرية موثقة في `batch43-review.jpg` ومواصفات وهوية وبصمات في `../assets/notes/batch43-generation-specs.json`. جميع الملفات الجديدة WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر لها بصمة ملف أو محتوى أو بصمة إدراكية مطابقة لملف آخر. نجح 33 اختبارًا للمكتبة والهوية.

الحالة الحالية: 48 صورة معتمدة في السجل الصارم، و762 نوتة بلا ملف، و1073 سجلًا قديمًا ينتظر حسم المطابقة، منها 18 سجلًا في مجموعات المشاركة التسع. اجتاز 1113 ملف WebP الفحص التقني. رُفع رقم كاش المكتبة إلى 71.

## نتيجة الدفعة 50

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch50-review.jpg` للتحقق من مطابقة الأشجار والأخشاب والمواد العطرية وعدم التكرار، دون حذف أي نوتة أو صورة سابقة. جميع الملفات WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية: 1215 نوتة بصورة محلية صالحة للعرض، و650 نوتة بلا ملف، ولا توجد صورة قديمة معادة إلى طابور التوليد. اجتاز 1225 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 80.

## نتيجة الدفعة 49

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch49-review.jpg` للتحقق من مطابقة أنواع الأخشاب والأشجار وعدم التكرار، دون حذف أي نوتة أو صورة سابقة. جميع الملفات WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية بعد سياسة الحفاظ على الصور المحلية: 1199 نوتة بصورة محلية صالحة للعرض، و666 نوتة بلا ملف، ولا توجد صورة قديمة معادة إلى طابور التوليد. اجتاز 1209 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 79.

## نتيجة الدفعة 48

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch48-review.jpg` للتحقق من مطابقة كل صورة للنوتة وعدم التكرار، دون حذف أي نوتة أو صورة سابقة. جميع الملفات WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية: 128 صورة معتمدة، و682 نوتة بلا ملف، و1055 صورة قديمة تنتظر المراجعة الدلالية. اجتاز 1193 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 77.

## نتيجة الدفعة 47

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch47-review.jpg` للتحقق من مطابقة كل صورة للنوتة وعدم التكرار، دون حذف أي نوتة أو صورة سابقة. جميع الملفات WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية: 112 صورة معتمدة، و698 نوتة بلا ملف، و1055 صورة قديمة تنتظر المراجعة الدلالية. اجتاز 1177 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 76.

## نتيجة الدفعة 46

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch46-review.jpg` للتحقق من مطابقة كل صورة للنوتة وعدم التكرار، دون حذف أي نوتة أو صورة سابقة. جميع الملفات WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية: 96 صورة معتمدة، و714 نوتة بلا ملف، و1055 صورة قديمة تنتظر المراجعة الدلالية. اجتاز 1161 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 75.

## نتيجة الدفعة 45

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، ثم روجعت بصريًا في `batch45-review.jpg` للتحقق من مطابقة كل صورة لاسم النوتة، مع إبقاء النوتات والصور السابقة وعدم حذفها. حُفظت الهوية والطلبات والبصمات في `../assets/notes/batch45-generation-specs.json`. جميع الملفات الجديدة WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر مطابقة SHA-256 أو content hash أو perceptual hash مع أي ملف آخر. نجح 34 اختبارًا للمكتبة والهوية.

الحالة الحالية: 80 صورة معتمدة في السجل الصارم، و730 نوتة بلا ملف، و1055 صورة قديمة تنتظر المراجعة الدلالية. اجتاز 1145 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 74.

## نتيجة الدفعة 44

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، مع مراجعة بصرية موثقة في `batch44-review.jpg` ومواصفات وهوية وبصمات في `../assets/notes/batch44-generation-specs.json`. جميع الملفات الجديدة WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر أي مطابقة SHA-256 أو content hash أو perceptual hash مع ملف آخر. نجح 33 اختبارًا للمكتبة والهوية وبصمات الأصول المعتمدة.

الحالة الحالية: 64 صورة معتمدة في السجل الصارم، و746 نوتة بلا ملف، و1055 سجلًا قديمًا ينتظر المراجعة الدلالية. إجمالي الهويات 1883 بعد فصل الهوية التي دُمجت سابقًا. اجتاز 1129 ملف WebP الفحص التقني، ورُفع رقم كاش المكتبة إلى 72.

## نتيجة الدفعة 42

أضيفت 16 صورة جديدة عبر طلبات ImageGen مستقلة، مع مراجعة بصرية موثقة في `batch42-review.jpg` وطلبات وهوية وبصمات في `../assets/notes/batch42-generation-specs.json`. جميع الملفات الجديدة WebP شفافة 320×320 وتحت 100 KiB، ولم تظهر بصمة مطابقة لملف آخر. نجح 33 اختبارًا للمكتبة والهوية وبصمات الأصول المعتمدة.

الحالة الحالية: 32 صورة معتمدة في السجل الصارم، و778 نوتة بلا ملف، و1073 سجلًا قديمًا ينتظر حسم المطابقة (منها 18 في مجموعات المشاركة التسع). إجمالي الهويات 1883 كما فُسر أدناه. اجتاز 1097 ملف WebP الفحص التقني. رُفع رقم كاش المكتبة إلى 70.

## نتيجة الدفعة 41

تم توليد 16 صورة بطلبات ImageGen مستقلة وربط كل نتيجة بمفتاح طلبها، ثم مراجعتها بصريًا وحفظها في `assets/notes/generated/`. لم تُستبدل ملفات قديمة. حُفظت الطلبات والهوية وبصمات الملفات ودليل المراجعة في `assets/notes/batch41-generation-specs.json`.

بعد الدفعة: 16 صورة معتمدة ضمن السجل الصارم الجديد، و794 نوتة بلا ملف صورة، و1073 سجلًا قديمًا ينتظر حسم المطابقة، بينها 18 سجلًا في مجموعات المشاركة التسع. يوجد الآن 1883 سجلًا بعد فصل الهوية التي دُمجت سابقًا. لا يعني عدد الملفات المتاحة أن الصور القديمة معتمدة.

جميع ملفات WebP البالغ عددها 1081 اجتازت الفحص التقني. لم تظهر للدفعة الجديدة بصمة ملف أو محتوى أو بصمة إدراكية مطابقة لملف آخر. لم يُنجز الفحص البصري لجميع الصور القديمة، ولم يُدّع ذلك.

## الحالة قبل التعديل

- 1882 سجل نوتة، و5 منتجات في قاعدة البيانات المحلية المفتوحة للقراءة فقط.
- 809 سجلات بلا صورة محلية متاحة.
- 9 مجموعات مشاركة صورة بين مفاتيح مختلفة، تضم 18 سجلًا. قد يمثل بعضها مرادفات فعلية؛ لم تُدمج تلقائيًا.
- 1055 سجلًا آخر بصور تحتاج مراجعة دلالية.
- عدد الصور المعتمدة بصريًا في هذا الفحص الأولي: صفر؛ لا يعني ذلك أن جميع الصور خاطئة.
- 1065 ملف WebP اجتازت فحص المقاس 320×320 والشفافية والحجم الأقل من 100 KiB.

## أسباب مثبتة في الكود

كانت `find()` تقبل أجزاء الأسماء باستخدام `includes` في الاتجاهين. احتوت المرادفات على دمج Fig Leaf مع Fig، وIris root مع Iris، وTaif Rose مع Rose، وغيرها من اختلافات النوع والجزء. كان `artwork()` يعرض أي رابط بلا دليل اعتماد. وكانت صور بعض بطاقات المنتج تُؤخذ مباشرة من بيانات المنتج قبل الرجوع إلى مكتبة النوتات. كذلك كان استيراد حالة النوتات يسمح بحفظ أعلام الاعتماد المرسلة من العميل.

## المعالجة

أزيلت المطابقة الجزئية من حل الهوية، وفُصلت المرادفات غير الصحيحة المحددة. أضيف canonicalKey ثابت مبني على slug، وقاموس مركزي للأجزاء المعرّفة؛ الأجزاء غير المعرفة تبقى ambiguous. تعطلت عمليات الدمج بين هويات مختلفة حتى توجد مراجعة صريحة للقاموس. تُحفظ الصور السابقة دون حذف، ويستخدم مسار العرض placeholder واضحًا عندما لا تتوفر أدلة مراجعة معتمدة.

تمت إضافة تحقق ملكية للأصول عبر URL وasset ID وSHA-256 وcontent hash وsource image ID وperceptual hash. تشابه البصمة الإدراكية مؤشر مراجعة، وليس إثباتًا لمعنى النوتة. لا يكشف النظام كل تعديل بصري ممكن ولا يدعي ذلك.

## حدود المراجعة المفتوحة

لم يكتمل الفحص البصري لجميع الملفات القديمة. أعداد same-plant-wrong-part وFruit/Leaf وSeed/Leaf وFlower/Fruit وWood/Leaf غير محسومة، وليست صفرًا مؤكدًا. لم يجر الوصول إلى LocalStorage في جلسة متصفح حية أو إلى قاعدة إنتاج بعيدة؛ راجع التقرير JSON لنطاق البيانات المحلية. لم تُستبدل صور النوتات الغامضة ولم تُعدّل قاعدة بيانات المستخدم.

فصل ورد طائفي عن الورد العام أعاد هوية مستقلة إلى القائمة؛ لذلك قد يزيد عدد السجلات عن 1882. هذا ليس توليد نوتة جديدة، بل تصحيح دمج سابق.

## ملفات الأدلة

- `note-image-audit-before.json`: لقطة قبل تعديل الربط.
- `note-image-audit-after.json`: القائمة بعد التصحيح وحالة كل صورة.
- `note-image-technical-audit.json`: SHA-256 والبصمات الإدراكية وفحوص الملفات.
- `../assets/notes/batch41-generation-specs.json`: مواصفات وطلبات الدفعة الجديدة عند اكتمالها.

## التحقق

اختبارات المكتبة والانحدار تغطي اختلاف هويات النباتات والأجزاء، رفض المطابقة الجزئية، تغيير الترتيب، رفض اعتماد العميل، ومنع مشاركة أصل بين هويتين مع السماح بإعادة استخدامه لنفس الهوية. اختبار قاعدة البيانات يستخدم قاعدة مؤقتة مستقلة. تم بناء runtime الفعلي للمتجر وفحص الصياغة وفق أمر typecheck؛ لم يُكتب app.min.js.
