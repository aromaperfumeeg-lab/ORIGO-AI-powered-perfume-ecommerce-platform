{
  "name": "origo-store",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "build": "echo ORIGO does not require a build step.",
    "build:knowledge": "node scripts/build-fragrance-knowledge.mjs",
    "check:i18n": "node scripts/check-i18n.mjs",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
