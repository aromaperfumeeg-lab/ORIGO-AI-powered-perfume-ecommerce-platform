{
  "name": "origo-store",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "build": "echo ORIGO does not require a build step.",
    "build:deploy": "pnpm run build && node scripts/build-deploy.mjs",
    "build:knowledge": "node scripts/build-fragrance-knowledge.mjs",
    "admin:reset-password": "node scripts/reset-admin-password.mjs --confirm-reset",
    "typecheck": "node --check db.mjs && node --check server.mjs && node --check scripts/reset-admin-password.mjs && node --check scripts/build-deploy.mjs",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
