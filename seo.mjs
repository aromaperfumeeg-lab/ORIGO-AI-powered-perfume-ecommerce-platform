const SITE_URL="https://origoscents.com";
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
const slug=value=>String(value??"").normalize("NFKD").toLowerCase().trim().replace(/[^\p{L}\p{N}]+/gu,"-").replace(/^-+|-+$/g,"");
const list=value=>Array.isArray(value)?value:[];
const published=products=>list(products).filter(product=>product?.status==="published"&&product.deleted!==true&&(product.category||"perfume")==="perfume");
export function productPath(product){return `/perfume/${encodeURIComponent(product.slug||slug([product.brandEn||product.brand,product.nameEn||product.nameAr].filter(Boolean).join("-"))||product.id)}`}
export function buildCanonicalUrl(pathname="/"){const clean=`/${String(pathname).split("?")[0].replace(/^\/+|\/+$/g,"")}`;return `${SITE_URL}${clean==="/"?"":clean}`}
export function buildImageAlt(product,language="ar"){return language==="ar"?`عطر ${product.nameAr||product.nameEn} من ${product.brandAr||product.brandEn||product.brand}`:`${product.nameEn||product.nameAr} by ${product.brandEn||product.brandAr||product.brand} perfume`}
export function buildProductSeo(product,language="ar"){
  const ar=language==="ar",name=ar?product.nameAr||product.nameEn:product.nameEn||product.nameAr,brand=ar?product.brandAr||product.brandEn||product.brand:product.brandEn||product.brandAr||product.brand,size=product.size||product.sizes?.[0]||"",concentration=product.concentration||"",manual=product.seo||{};
  const fallbackTitle=ar?`${name} من ${brand}${size?` ${size}`:""} | عطر أصلي في مصر | ORIGO`:`${brand} ${name}${size?` ${size}`:""} Original Perfume Egypt | ORIGO`;
  const fallbackDescription=ar?`تسوق عطر ${name} الأصلي من ${brand} في مصر${size?`، ${size}`:""}${concentration?`، ${concentration}`:""}. تعرف على النوتات والثبات والفوحان والسعر والتوفر لدى ORIGO.`:`Shop original ${name} by ${brand} in Egypt${size?`, ${size}`:""}${concentration?`, ${concentration}`:""}. Explore notes, longevity, projection, price and availability at ORIGO.`;
  const image=list(product.images).map(item=>typeof item==="string"?item:item?.url).find(Boolean);return Object.freeze({title:manual.title||fallbackTitle,description:manual.description||(ar?product.descriptionAr:product.descriptionEn)||fallbackDescription,canonical:buildCanonicalUrl(productPath(product)),robots:"index,follow",keywords:list(manual.keywords),image:image?new URL(image,SITE_URL).href:"",imageAlt:buildImageAlt(product,language)});
}
export function buildProductStructuredData(product){
  const seo=buildProductSeo(product),quantity=product.inventory?.quantity,available=product.status!=="unavailable"&&(quantity==null||Number(quantity)>0),images=list(product.images).map(image=>typeof image==="string"?image:image?.url).filter(Boolean).map(url=>new URL(url,SITE_URL).href),data={"@context":"https://schema.org","@type":"Product",name:product.nameAr||product.nameEn,description:seo.description,image:images,sku:product.sku||undefined,brand:{"@type":"Brand",name:product.brandAr||product.brandEn||product.brand},offers:{"@type":"Offer",url:seo.canonical,priceCurrency:"EGP",price:Number(product.price),availability:`https://schema.org/${available?"InStock":"OutOfStock"}`,itemCondition:"https://schema.org/NewCondition"},url:seo.canonical};
  if(product.barcode)data.gtin=String(product.barcode);const rating=Number(product.reviewSummary?.average??product.rating),count=Number(product.reviewSummary?.count);if(rating>0&&count>0)data.aggregateRating={"@type":"AggregateRating",ratingValue:rating,reviewCount:count};return data;
}
export function buildBreadcrumbStructuredData(items){return{"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:items.map((item,index)=>({"@type":"ListItem",position:index+1,name:item.name,item:buildCanonicalUrl(item.path)}))}}
export function buildBrandSeo(brand,count){return{title:`عطور ${brand} الأصلية في مصر | الأسعار والمنتجات | ORIGO`,description:`تسوق عطور ${brand} الأصلية في مصر من ORIGO واكتشف المنتجات والأسعار والتوفر.`,canonical:buildCanonicalUrl(`/brands/${slug(brand)}`),robots:count?"index,follow":"noindex,follow"}}
export function buildCategorySeo(type,label,count){return{title:`${label} الأصلية في مصر | ORIGO`,description:`اكتشف ${label} المتوفرة فعليًا لدى ORIGO مع الأسعار وبيانات العطر.`,canonical:buildCanonicalUrl(`/perfumes/${type}`),robots:count?"index,follow":"noindex,follow"}}
function productByPath(products,path){const wanted=decodeURIComponent(path.split("/").filter(Boolean)[1]||"");return published(products).find(product=>(product.slug||slug([product.brandEn||product.brand,product.nameEn||product.nameAr].filter(Boolean).join("-"))||String(product.id))===wanted)}
export function seoForRoute(pathname,products=[]){
  const items=published(products),path=String(pathname||"/");if(path.startsWith("/search")||path.startsWith("/fragrance-finder/results"))return{title:"ORIGO",description:"",canonical:buildCanonicalUrl(path.startsWith("/search")?"/perfumes":"/fragrance-finder/for-whom"),robots:"noindex,follow",jsonLd:[]};
  if(path.startsWith("/perfume/")){const product=productByPath(items,path);if(!product)return{title:"العطر غير موجود | ORIGO",description:"",canonical:buildCanonicalUrl(path),robots:"noindex,follow",jsonLd:[]};const seo=buildProductSeo(product);return{...seo,jsonLd:[buildProductStructuredData(product),buildBreadcrumbStructuredData([{name:"الرئيسية",path:"/"},{name:"العطور",path:"/perfumes"},{name:product.brandAr||product.brand,path:`/brands/${slug(product.brandEn||product.brand)}`},{name:product.nameAr||product.nameEn,path:productPath(product)}])]};}
  const brandMatch=path.match(/^\/brands\/([^/]+)\/?$/);if(brandMatch){const matches=items.filter(product=>slug(product.brandEn||product.brandAr||product.brand)===brandMatch[1]),brand=matches[0]?.brandAr||matches[0]?.brandEn||matches[0]?.brand||brandMatch[1];return{...buildBrandSeo(brand,matches.length),jsonLd:[]};}
  const categoryMatch=path.match(/^\/perfumes\/(men|women|unisex|edp|edt|winter|autumn|spring|summer|day|night)\/?$/);if(categoryMatch){const key=categoryMatch[1],scoreKey=["winter","autumn","spring","summer"].includes(key)?"seasonScores":["day","night"].includes(key)?"usageTimeScores":null;const matches=items.filter(product=>scoreKey?product[scoreKey]?.[key]!=null:key==="edp"||key==="edt"?String(product.concentration).toLowerCase()===key:product.gender===key);return{...buildCategorySeo(key,{men:"عطور رجالية",women:"عطور نسائية",unisex:"عطور للجنسين",edp:"عطور EDP",edt:"عطور EDT",winter:"عطور شتوية",autumn:"عطور خريفية",spring:"عطور ربيعية",summer:"عطور صيفية",day:"عطور نهارية",night:"عطور ليلية"}[key],matches.length),jsonLd:[]};}
  const noteMatch=path.match(/^\/notes\/([^/]+)\/?$/);if(noteMatch){const key=noteMatch[1],matches=items.filter(product=>Object.values(product.noteSelectionsBundle||{}).flat().some(note=>[note?.ar,note?.en,note?.nameAr,note?.nameEn].some(name=>slug(name)===key)));return{title:`عطور ${key} الأصلية في مصر | ORIGO`,description:`اكتشف العطور الأصلية التي تحتوي على نوتة ${key} والمتوفرة لدى ORIGO.`,canonical:buildCanonicalUrl(path),robots:matches.length>=2?"index,follow":"noindex,follow",jsonLd:[]};}
  return{title:"ORIGO | العطور الأصلية في مصر",description:"اكتشف العطور الأصلية للرجال والنساء وللجنسين في مصر من ORIGO.",canonical:buildCanonicalUrl(path==="/"?"/":path),robots:path==="/"||path==="/perfumes"?"index,follow":"noindex,follow",jsonLd:[]};
}
export function injectSeoIntoHtml(html,seo){const imageTags=seo.image?`<meta property="og:image" content="${esc(seo.image)}"><meta property="og:image:alt" content="${esc(seo.imageAlt||"")}"><meta name="twitter:image" content="${esc(seo.image)}">`:"";const tags=`<meta name="robots" content="${esc(seo.robots)}"><link rel="canonical" href="${esc(seo.canonical)}"><meta property="og:type" content="website"><meta property="og:title" content="${esc(seo.title)}"><meta property="og:description" content="${esc(seo.description)}"><meta property="og:url" content="${esc(seo.canonical)}">${imageTags}<meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${esc(seo.title)}"><meta name="twitter:description" content="${esc(seo.description)}">${list(seo.jsonLd).map(data=>`<script type="application/ld+json">${JSON.stringify(data).replace(/</g,"\\u003c")}</script>`).join("")}`;const clean=html.replace(/<link\s+rel="canonical"[^>]*>/gi,"").replace(/<meta\s+(?:property="og:[^"]+"|name="twitter:[^"]+")[^>]*>/gi,"").replace(/<meta\s+name="robots"[^>]*>/gi,"");return clean.replace(/<title>[\s\S]*?<\/title>/i,`<title>${esc(seo.title)}</title>`).replace(/<meta\s+name="description"[\s\S]*?>/i,`<meta name="description" content="${esc(seo.description)}">`).replace("</head>",`${tags}</head>`)}
export function buildSitemap(products=[]){const urls=new Map([[buildCanonicalUrl("/"),null],[buildCanonicalUrl("/perfumes"),null]]);for(const product of published(products)){urls.set(buildCanonicalUrl(productPath(product)),product.updatedAt||product.updated_at||null);const brand=product.brandEn||product.brandAr||product.brand;if(brand)urls.set(buildCanonicalUrl(`/brands/${slug(brand)}`),null);for(const key of ["winter","autumn","spring","summer"])if(product.seasonScores?.[key]!=null)urls.set(buildCanonicalUrl(`/perfumes/${key}`),null);for(const key of ["day","night"])if(product.usageTimeScores?.[key]!=null)urls.set(buildCanonicalUrl(`/perfumes/${key}`),null);if(["men","women","unisex"].includes(product.gender))urls.set(buildCanonicalUrl(`/perfumes/${product.gender}`),null);if(["edp","edt"].includes(String(product.concentration).toLowerCase()))urls.set(buildCanonicalUrl(`/perfumes/${String(product.concentration).toLowerCase()}`),null)}return`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[...urls].map(([loc,lastmod])=>`<url><loc>${esc(loc)}</loc>${lastmod?`<lastmod>${esc(String(lastmod).slice(0,10))}</lastmod>`:""}</url>`).join("")}</urlset>`}
export function robotsTxt(){return`User-agent: *\nAllow: /\nDisallow: /admin\nDisallow: /account\nDisallow: /checkout\nDisallow: /cart\nDisallow: /search\nSitemap: ${SITE_URL}/sitemap.xml\n`}
